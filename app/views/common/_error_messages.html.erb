<% if target.errors.any? %>
  <div>
    <h2><%= pluralize(target.errors.count, "error") %> prohibited this record from being saved:</h2>
      <ul>
        <% target.errors.messages.each do |message| %>
          <li><%= message[1].last %></li>
        <% end %>
      </ul>
  </div>
<% end %>
