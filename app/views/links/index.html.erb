<%= link_to "Submit a new link", new_link_path %>

<div>
  <% @links.each do |link| %>
    <%= link_to link.title, link.url, target: '_blank' %> | <%= link_to '參與討論', link_path(link) %>
  <% end %>
</div>
