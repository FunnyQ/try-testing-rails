<%= link_to "Submit a new link", new_link_path %>

<div>
  <% @links.each do |link| %>
    <%= link_to link.title, link_path(link) %> | <%= link_to '前往連結', link.url, target: '_blank' %>
  <% end %>
</div>
